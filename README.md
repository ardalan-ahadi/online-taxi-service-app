# Setup Guide
- initiate a database folder and copy the files
- initiate an express.js project for backend and copy the files as structured
- initiate an expo project named "frontend" and copy the code files there
	- expo initiates "node_modules" folder and installs primary libraries
	- u should install the missing ones using npm in command prompt
- update the addresses (back & front) & links (front)
- install missing requirments of backend and database too
