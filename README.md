language:
javascript node

frameworks:
backend: "express"
frontend: "react-native"
database: "sqlite3"

notes:
1- initiate a database folder and copy the files
2- initiate an express.js project for backend and copy the files as structured
3- initiate an expo project named "frontend" and copy the code files there
	* expo initiates "node_modules" folder and installs primary libraries
	* u should install the missing ones using npm in command prompt
4 update the addresses (back & front) & links (front)
5 install missing requirments of backend and database too
